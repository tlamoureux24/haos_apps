"""Dependency-free administration UI assets served only by the Ingress listener."""

ADMIN_CSS = """
:root{color-scheme:light;--bg:#f3f6f9;--surface:#fff;--surface2:#edf3f7;--text:#17243a;--muted:#687b90;--line:#d8e2eb;--navy:#142f4b;--cyan:#058caf;--cyan-soft:#dff5fa;--amber:#e9a72f;--amber-soft:#fff5df;--good:#16845b;--bad:#c43f55;--shadow:0 5px 22px #1e3a5f12}
html[data-theme=dark]{color-scheme:dark;--bg:#0d1420;--surface:#172235;--surface2:#202c41;--text:#e7edf5;--muted:#a4b2c6;--line:#30415d;--navy:#101b2a;--cyan:#52c9e6;--cyan-soft:#173b4a;--amber:#f0b84e;--amber-soft:#493b20;--good:#50c895;--bad:#ff8293;--shadow:none}
[hidden]{display:none!important}
*{box-sizing:border-box}body{font:15px system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;background:var(--bg);color:var(--text);margin:0}.app{max-width:1440px;margin:auto;padding:24px}.top{display:flex;align-items:center;gap:20px;margin-bottom:26px}.brand{display:flex;align-items:center;gap:11px;color:var(--cyan);text-decoration:none;font-size:24px;font-weight:800;margin-right:auto;letter-spacing:-.02em}.brand img{width:46px;height:46px;object-fit:contain;filter:drop-shadow(0 4px 8px #102a4330)}.brand small{display:block;color:var(--muted);font-size:11px;font-weight:650;letter-spacing:.08em;text-transform:uppercase}.nav{display:flex;align-items:center;gap:5px}.nav a,.theme{color:var(--text);text-decoration:none;padding:9px 12px;border-radius:7px;border:0;background:transparent;font:inherit}.nav .active{background:var(--cyan-soft);color:var(--cyan);font-weight:700}.theme{background:var(--surface);border:1px solid var(--line);cursor:pointer;font-size:17px}.view{display:none}.view.active{display:block}.hero{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:24px;align-items:center;margin:4px 0 20px}.hero h1,.pagehead h1{font-size:29px;margin:0 0 5px;letter-spacing:-.025em}.hero p,.pagehead p{margin:0;color:var(--muted)}.pagehead{margin:4px 0 20px}.pagehead.split{display:flex;align-items:center;justify-content:space-between;gap:18px}.export{color:#fff;background:var(--cyan);text-decoration:none;padding:10px 14px;border-radius:7px;font-weight:700;white-space:nowrap}.health{display:flex;align-items:center;gap:8px;color:var(--good);font-weight:700;background:var(--surface);border:1px solid var(--line);border-radius:10px;padding:11px 14px;box-shadow:var(--shadow)}.health i{width:9px;height:9px;border-radius:50%;background:currentColor;box-shadow:0 0 0 4px color-mix(in srgb,currentColor 15%,transparent)}.health a{color:inherit;text-decoration:none}.metrics{display:grid;grid-template-columns:repeat(3,minmax(150px,1fr));gap:12px;margin:0 0 16px}.metric{background:var(--surface);border:1px solid var(--line);border-radius:12px;padding:18px;box-shadow:var(--shadow);position:relative;overflow:hidden}.metric:before{content:"";position:absolute;left:0;top:0;bottom:0;width:3px;background:var(--cyan)}.metric.amber:before{background:var(--amber)}.metric strong{display:block;font-size:29px;font-variant-numeric:tabular-nums;line-height:1.1}.metric span{display:block;color:var(--muted);margin-top:5px}.workspace{display:grid;grid-template-columns:minmax(310px,390px) minmax(0,1fr);gap:12px}.card{background:var(--surface);border:1px solid var(--line);border-radius:12px;padding:20px;box-shadow:var(--shadow)}.cardhead{display:flex;align-items:flex-start;justify-content:space-between;gap:16px;margin-bottom:18px}.card h2{margin:0 0 4px;font-size:19px}.cardhead p{margin:0;color:var(--muted);font-size:13px}.count{background:var(--surface2);color:var(--muted);border-radius:999px;padding:4px 9px;font-size:12px}label{display:block;font-weight:650;margin:13px 0 5px}input,select{display:block;width:100%;padding:10px 11px;color:var(--text);background:var(--surface);border:1px solid var(--line);border-radius:7px;outline:none}input:focus,select:focus{border-color:var(--cyan);box-shadow:0 0 0 3px var(--cyan-soft)}fieldset{border:0;padding:0;margin:16px 0}legend{font-weight:700;margin-bottom:8px}.permission{display:flex;align-items:flex-start;gap:9px;padding:8px 9px;margin:0;border-radius:7px;font-weight:400;cursor:pointer}.permission:hover{background:var(--surface2)}.permission input{width:auto;margin:3px 0}.permission span{display:block}.permission small{display:block;color:var(--muted);margin-top:2px}button.primary,.danger{border:0;border-radius:7px;padding:10px 14px;font-weight:700;cursor:pointer}.primary{background:var(--cyan);color:#fff;width:100%}.danger{background:transparent;color:var(--bad);border:1px solid color-mix(in srgb,var(--bad) 45%,var(--line));padding:7px 10px}.danger:hover{background:color-mix(in srgb,var(--bad) 9%,transparent)}button:disabled{cursor:not-allowed;opacity:.55}.identities{min-height:300px}.identity{border-top:1px solid var(--line);padding:16px 0}.identity:first-child{border-top:0;padding-top:0}.identity:last-child{padding-bottom:0}.identityhead{display:flex;justify-content:space-between;gap:12px}.identityname{font-weight:750}.identitymeta{color:var(--muted);font-size:13px;margin-top:3px}.pill{display:inline-block;padding:3px 8px;border-radius:999px;background:var(--cyan-soft);color:var(--cyan);font-size:12px}.pill.revoked{background:color-mix(in srgb,var(--bad) 12%,var(--surface));color:var(--bad)}.actions{font:12px ui-monospace,SFMono-Regular,Consolas,monospace;color:var(--muted);overflow-wrap:anywhere}.empty{text-align:center;padding:45px 18px;color:var(--muted)}.emptyicon{display:grid;place-items:center;width:52px;height:52px;margin:0 auto 12px;border-radius:14px;background:var(--surface2);color:var(--cyan);font-size:23px}.credential{display:none;margin-top:16px;padding:15px;background:var(--amber-soft);border:1px solid color-mix(in srgb,var(--amber) 55%,var(--line));border-radius:9px}.credential.show{display:block}.credential code{display:block;overflow-wrap:anywhere;margin:10px 0;padding:10px;background:var(--surface);border-radius:6px}.credential strong{color:var(--text)}.credential .danger{display:block;margin-top:12px}.error{color:var(--bad)}.loading{color:var(--muted)}.tablewrap{overflow:auto;border:1px solid var(--line);border-radius:10px}table{width:100%;border-collapse:collapse;white-space:nowrap}th,td{text-align:left;padding:11px 12px;border-bottom:1px solid var(--line)}th{font-size:12px;color:var(--muted);background:var(--surface2)}tr:last-child td{border:0}td.wrap{white-space:normal;min-width:220px}details summary{color:var(--cyan);cursor:pointer;font-weight:650}pre{white-space:pre-wrap;overflow-wrap:anywhere;background:var(--surface2);padding:12px;border-radius:7px;font-size:12px;max-width:650px}.state{display:inline-block;padding:3px 8px;border-radius:999px;background:var(--amber-soft);color:var(--amber);font-size:12px}.state.completed,.state.allowed,.state.ready{background:var(--cyan-soft);color:var(--good)}.state.denied,.state.unreachable,.state.invalid{background:color-mix(in srgb,var(--bad) 12%,var(--surface));color:var(--bad)}.connector-actions{display:flex;flex-wrap:wrap;gap:7px}.connector-actions .primary{width:auto;padding:7px 10px}.connector-inventory{margin-top:14px}.connector-inventory .tablewrap{max-height:420px}
@media(max-width:850px){.app{padding:16px}.top{align-items:flex-start;flex-wrap:wrap}.brand{width:100%}.nav{max-width:100%;overflow-x:auto}.hero{grid-template-columns:1fr}.health{justify-self:start}.workspace{grid-template-columns:1fr}.metrics{grid-template-columns:repeat(3,1fr)}}
@media(max-width:560px){.metrics{grid-template-columns:1fr}.hero h1{font-size:25px}.nav .future{display:none}}
.brand b{font-size:13px;color:var(--muted);font-weight:650;white-space:nowrap}.status-dot{display:block;width:12px;height:12px;border-radius:50%;background:var(--bad);box-shadow:0 0 0 4px color-mix(in srgb,var(--bad) 13%,transparent);margin:7px 4px}.status-dot.ready,.status-dot.active{background:var(--good);box-shadow:0 0 0 4px color-mix(in srgb,var(--good) 13%,transparent)}.status-dot.disabled{background:var(--muted);box-shadow:0 0 0 4px color-mix(in srgb,var(--muted) 13%,transparent)}.state.unavailable{background:color-mix(in srgb,var(--bad) 12%,var(--surface));color:var(--bad)}textarea{display:block;width:100%;padding:10px 11px;color:var(--text);background:var(--surface);border:1px solid var(--line);border-radius:7px;resize:vertical;font:inherit}.tool-picker-row{display:grid;grid-template-columns:1fr 1.35fr auto;gap:8px;align-items:end}.tool-picker-row label{margin-top:0}.tool-picker-row button{margin-bottom:0;height:39px}.selected-tools{margin-top:10px;display:grid;gap:6px}.selected-tool{display:flex;align-items:center;justify-content:space-between;gap:10px;padding:8px 10px;border:1px solid var(--line);border-radius:7px;background:var(--surface2)}.selected-tool span{min-width:0;overflow-wrap:anywhere;flex:1}.selected-tool small{color:var(--muted)}.selected-tool .danger{padding:4px 8px}.task-workspace{grid-template-columns:minmax(350px,480px) minmax(0,1fr)}.report{min-width:360px;max-width:760px}.report-summary{font-weight:650;line-height:1.45;margin-bottom:10px}.findings{margin:8px 0 12px;padding-left:20px}.findings li{margin:8px 0;line-height:1.4}.finding-meta{color:var(--muted);font-size:12px;text-transform:uppercase;letter-spacing:.04em}.report details{margin-top:8px}@media(max-width:850px){.task-workspace{grid-template-columns:1fr}}@media(max-width:560px){.tool-picker-row{grid-template-columns:1fr}.tool-picker-row button{width:100%}.report{min-width:260px}}
.danger-metric:before{background:var(--bad)}.job-toolbar{display:flex;gap:7px;margin-bottom:14px}.job-filter{border:1px solid var(--line);border-radius:7px;padding:7px 11px;background:var(--surface);color:var(--muted);font:inherit;font-weight:650;cursor:pointer}.job-filter.active{color:var(--cyan);border-color:var(--cyan);background:var(--cyan-soft)}
"""

ADMIN_JS = r"""
const root=document.querySelector('main'),base=root.dataset.base,csrf=root.dataset.csrf;
const themeButton=document.querySelector('#theme');
const setTheme=value=>{document.documentElement.dataset.theme=value;localStorage.setItem('agw-theme',value);themeButton.textContent=value==='dark'?'☀':'☾';themeButton.title=value==='dark'?'Mode clair':'Mode sombre'};
setTheme(localStorage.getItem('agw-theme')||(matchMedia('(prefers-color-scheme: dark)').matches?'dark':'light'));
themeButton.onclick=()=>setTheme(document.documentElement.dataset.theme==='dark'?'light':'dark');
const api=(path,options={})=>fetch(base+path,{...options,headers:{'X-CSRF-Token':csrf,'Content-Type':'application/json',...(options.headers||{})}}).then(async r=>{const body=await r.json();if(!r.ok)throw new Error(body.error?.code||`HTTP ${r.status}`);return body});
const esc=value=>String(value??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const date=value=>value?new Intl.DateTimeFormat('fr-FR',{dateStyle:'medium',timeStyle:'short'}).format(new Date(value)):'Jamais';
const eventOutcome=value=>({accepted:'Exécution créée',queued:'Exécution créée',task_execution_active:'Exécution déjà active',cooldown_active:'Cooldown actif',grace_started:'Grâce démarrée',grace_active:'Grâce déjà active',grace_cancelled:'Grâce annulée',recovery_recorded:'Rétablissement enregistré',accepted_after_grace:'Exécution après grâce'}[value]||value||'Inconnu');
const empty=(icon,title,text)=>`<div class="empty"><div class="emptyicon">${icon}</div><strong>${title}</strong><p>${text}</p></div>`;
const table=(headers,rows)=>rows.length?`<table><thead><tr>${headers.map(h=>`<th>${h}</th>`).join('')}</tr></thead><tbody>${rows.join('')}</tbody></table>`:'';
const renderFinding=item=>{if(typeof item==='string')return `<li>${esc(item)}</li>`;if(!item||typeof item!=='object')return `<li>${esc(String(item))}</li>`;const title=item.finding??item.summary??item.message??item.title??'Constat',meta=[item.severity,item.category].filter(Boolean).join(' · '),evidence=item.evidence===undefined?'':`<details><summary>Preuves</summary><pre>${esc(JSON.stringify(item.evidence,null,2))}</pre></details>`;return `<li>${meta?`<div class="finding-meta">${esc(meta)}</div>`:''}${esc(title)}${evidence}</li>`};
const renderReport=report=>`<div class="report"><div class="report-summary">${esc(report?.summary||'Rapport sans résumé.')}</div>${Array.isArray(report?.findings)&&report.findings.length?`<strong>Constats</strong><ul class="findings">${report.findings.map(renderFinding).join('')}</ul>`:'<p class="loading">Aucun constat détaillé.</p>'}<details><summary>Afficher les données brutes</summary><pre>${esc(JSON.stringify(report,null,2))}</pre></details></div>`;
async function loadPanel(id,path,key,headers,renderRows,emptyState){const panel=document.querySelector(id);try{const data=await api(path);panel.innerHTML=table(headers,renderRows(data[key]))||empty(...emptyState)}catch(error){panel.innerHTML=empty('!','Chargement impossible',`La vue est temporairement indisponible (${esc(error.message)}).`)}}
let jobFilter='all';
async function loadJobs(){const panel=document.querySelector('#jobs-list');try{const data=await api('/admin/api/v1/jobs'),items=data.jobs.filter(j=>jobFilter==='all'||(jobFilter==='active'?['queued','leased'].includes(j.state):j.state===jobFilter));panel.innerHTML=table(['Créée','Tâche','État','Tentatives','Dernier résultat','Événement','Rapports','Action'],items.map(j=>`<tr><td>${esc(date(j.created_at))}</td><td>${esc(j.task_name)}</td><td><span class="state ${j.state}">${esc(j.state)}</span></td><td>${esc(j.attempt_count)}</td><td class="wrap">${esc(j.last_failure_reason||j.last_attempt_outcome||'—')}</td><td>${esc(j.event_id||'—')}</td><td>${esc(j.report_count)}</td><td>${j.state==='queued'?`<button class="danger cancel-job" data-id="${esc(j.id)}">Annuler</button>`:j.state==='dead_letter'?`<button class="primary requeue-job" data-id="${esc(j.id)}">Relancer</button>`:'—'}</td></tr>`))||empty('◇',jobFilter==='all'?'Aucune exécution':'Aucun résultat','Aucune exécution ne correspond à ce filtre.');panel.querySelectorAll('.cancel-job').forEach(button=>button.onclick=async()=>{if(confirm('Annuler définitivement cette tâche en attente ?')){try{await api('/admin/api/v1/jobs/cancel',{method:'POST',body:JSON.stringify({job_id:button.dataset.id})});await Promise.all([loadJobs(),refresh()])}catch(error){alert(`Annulation impossible : ${error.message}`)}}});panel.querySelectorAll('.requeue-job').forEach(button=>button.onclick=async()=>{if(confirm('Créer une nouvelle exécution à partir de cette dead letter ?')){button.disabled=true;try{await api('/admin/api/v1/jobs/requeue',{method:'POST',body:JSON.stringify({job_id:button.dataset.id})});await Promise.all([loadJobs(),loadTaskComposer(),refresh()])}catch(error){button.disabled=false;alert(`Relance impossible : ${error.message}`)}}})}catch(error){panel.innerHTML=empty('!','Chargement impossible',`La vue est temporairement indisponible (${esc(error.message)}).`)}}
async function loadOperationalViews(){await Promise.all([
loadPanel('#events-list','/admin/api/v1/events','events',['Reçu','Type','Source','Résultat','Sujet','Détail'],items=>items.map(e=>`<tr><td>${esc(date(e.received_at))}</td><td><span class="pill">${esc(e.event_type)}</span></td><td>${esc(e.source_name)}</td><td><span class="state ${e.job_id?'completed':'queued'}">${esc(eventOutcome(e.outcome))}</span></td><td><code>${esc(JSON.stringify(e.payload?.subject||{}))}</code></td><td><details><summary>Afficher</summary><pre>${esc(JSON.stringify(e.payload,null,2))}</pre></details></td></tr>`),['↯','Aucun événement','Les événements authentifiés apparaîtront ici.']),
loadJobs(),
loadPanel('#reports-list','/admin/api/v1/reports','reports',['Créé','Tâche','Rapport'],items=>items.map(r=>`<tr><td>${esc(date(r.created_at))}</td><td>${esc(r.task_name)}</td><td class="wrap">${renderReport(r.report)}</td></tr>`),['▤','Aucun rapport','Les résultats structurés produits par les agents apparaîtront ici.']),
loadPanel('#audit-list','/admin/api/v1/audit','audit_entries',['Date','Décision','Action','Acteur','Motif','Contexte'],items=>items.map(a=>`<tr><td>${esc(date(a.occurred_at))}</td><td><span class="state ${esc(a.decision)}">${esc(a.decision)}</span></td><td>${esc(a.action)}</td><td>${esc(a.actor_name||'Système / inconnu')}</td><td>${esc(a.reason_code)}</td><td><details><summary>Afficher</summary><pre>${esc(JSON.stringify({correlation_id:a.correlation_id,target_type:a.target_type,target_id:a.target_id,metadata:a.metadata},null,2))}</pre></details></td></tr>`),['⌁','Journal vide','Les décisions de sécurité apparaîtront ici.'])])}
async function loadConnectors(){const panel=document.querySelector('#connector-list');try{const data=await api('/admin/api/v1/connectors');document.querySelector('#connector-count').textContent=data.count;panel.innerHTML=data.connectors.length?data.connectors.map(c=>`<article class="identity connector" data-id="${esc(c.id)}"><div class="identityhead"><div><div class="identityname">${esc(c.display_name)}</div><div class="identitymeta">${esc(c.display_endpoint)} · vérifié ${esc(date(c.last_checked_at))}</div></div><i class="status-dot ${esc(c.status)}" role="img" aria-label="${esc(c.status)}" title="${esc(c.status)}"></i></div><p>${esc(c.tool_count)} outil(s) découvert(s) · inventaire ${esc(c.inventory_revision)}</p><div class="connector-actions"><button class="danger connector-tools" type="button">Voir les outils</button><button class="danger connector-check" type="button">Vérifier</button><button class="danger connector-toggle" type="button">${c.enabled?'Désactiver':'Activer'}</button><button class="danger connector-delete" type="button">Supprimer</button></div><div class="connector-inventory"></div></article>`).join(''):empty('⌁','Aucun connecteur','Ajoutez un serveur MCP lorsque vous en avez besoin.');panel.querySelectorAll('.connector').forEach(card=>{const id=card.dataset.id;card.querySelector('.connector-tools').onclick=async event=>{const target=card.querySelector('.connector-inventory');if(target.dataset.open==='true'){target.innerHTML='';target.dataset.open='false';event.currentTarget.textContent='Voir les outils';return}target.dataset.open='true';event.currentTarget.textContent='Masquer les outils';target.innerHTML='<p class="loading">Chargement…</p>';try{const result=await api(`/admin/api/v1/connectors/tools?connector_id=${encodeURIComponent(id)}`);target.innerHTML=result.tools.length?table(['Outil','Description','Schéma'],result.tools.map(t=>`<tr><td><code>${esc(t.name)}</code></td><td class="wrap">${esc(t.description||'—')}</td><td><details><summary>Afficher</summary><pre>${esc(JSON.stringify(t.input_schema,null,2))}</pre></details></td></tr>`)):empty('◇','Aucun outil','Ce serveur MCP ne publie aucun outil.')}catch(error){target.innerHTML=empty('!','Inventaire indisponible',esc(error.message))}};card.querySelector('.connector-check').onclick=async()=>{try{await api('/admin/api/v1/connectors/check',{method:'POST',body:JSON.stringify({connector_id:id})})}catch(error){alert(`Connecteur indisponible : ${error.message}`)}await Promise.all([loadConnectors(),loadTaskComposer()])};card.querySelector('.connector-toggle').onclick=async()=>{const connector=data.connectors.find(item=>item.id===id);await api('/admin/api/v1/connectors/enabled',{method:'POST',body:JSON.stringify({connector_id:id,enabled:!connector.enabled})});await Promise.all([loadConnectors(),loadTaskComposer()])};card.querySelector('.connector-delete').onclick=async()=>{if(confirm('Supprimer ce connecteur et son inventaire ?')){try{await api('/admin/api/v1/connectors/delete',{method:'POST',body:JSON.stringify({connector_id:id})});await Promise.all([loadConnectors(),loadTaskComposer()])}catch(error){alert(`Suppression impossible : ${error.message}`)}}}})}catch(error){panel.innerHTML=empty('!','Chargement impossible',esc(error.message))}}
let taskToolGroups=[],selectedTaskTools=[];
function renderSelectedTaskTools(){const panel=document.querySelector('#selected-task-tools');panel.innerHTML=selectedTaskTools.length?selectedTaskTools.map((item,index)=>`<div class="selected-tool"><span><small>${esc(item.connector_name)}</small><br><code>${esc(item.tool_name)}</code></span><button class="danger remove-task-tool" type="button" data-index="${index}" aria-label="Retirer ${esc(item.tool_name)}">Retirer</button></div>`).join(''):'<small class="loading">Aucun outil ajouté.</small>';panel.querySelectorAll('.remove-task-tool').forEach(button=>button.onclick=()=>{selectedTaskTools.splice(Number(button.dataset.index),1);renderSelectedTaskTools()})}
function updateTaskToolSelect(){const connectorId=document.querySelector('#task-connector-select').value,group=taskToolGroups.find(item=>item.connector.id===connectorId),select=document.querySelector('#task-tool-select');select.innerHTML=group?.tools.length?group.tools.map(tool=>`<option value="${esc(tool.name)}">${esc(tool.name)}</option>`).join(''):'<option value="">Aucun outil disponible</option>';select.disabled=!group?.tools.length;document.querySelector('#add-task-tool').disabled=!group?.tools.length}
async function loadTaskComposer(){const [connectorData,taskData]=await Promise.all([api('/admin/api/v1/connectors'),api('/admin/api/v1/tasks')]);const ready=connectorData.connectors.filter(c=>c.enabled&&c.status==='ready');taskToolGroups=await Promise.all(ready.map(async c=>({connector:c,tools:(await api(`/admin/api/v1/connectors/tools?connector_id=${encodeURIComponent(c.id)}`)).tools})));selectedTaskTools=selectedTaskTools.filter(item=>taskToolGroups.some(group=>group.connector.id===item.connector_id&&group.tools.some(tool=>tool.name===item.tool_name)));const picker=document.querySelector('#task-tool-picker');picker.innerHTML=taskToolGroups.length?`<div class="tool-picker-row"><label>Connecteur<select id="task-connector-select">${taskToolGroups.map(group=>`<option value="${esc(group.connector.id)}">${esc(group.connector.display_name)}</option>`).join('')}</select></label><label>Outil<select id="task-tool-select"></select></label><button id="add-task-tool" class="danger" type="button">Ajouter</button></div><div id="selected-task-tools" class="selected-tools"></div>`:empty('⌁','Aucun connecteur opérationnel','Ajoutez ou réactivez au moins un connecteur MCP.');if(taskToolGroups.length){document.querySelector('#task-connector-select').onchange=updateTaskToolSelect;document.querySelector('#add-task-tool').onclick=()=>{const connectorId=document.querySelector('#task-connector-select').value,toolName=document.querySelector('#task-tool-select').value,group=taskToolGroups.find(item=>item.connector.id===connectorId);if(toolName&&!selectedTaskTools.some(item=>item.connector_id===connectorId&&item.tool_name===toolName)){selectedTaskTools.push({connector_id:connectorId,connector_name:group.connector.display_name,tool_name:toolName});renderSelectedTaskTools()}};updateTaskToolSelect();renderSelectedTaskTools()}document.querySelector('#task-count').textContent=taskData.count;document.querySelector('#task-list').innerHTML=taskData.tasks.length?taskData.tasks.map(t=>`<article class="identity"><div class="identityhead"><div><div class="identityname">${esc(t.display_name)}</div><div class="identitymeta">Révision ${esc(t.revision)} · ${esc(t.tools.length)} outil(s)</div></div><i class="status-dot ${esc(t.status)}" role="img" aria-label="${esc(t.status)}" title="${esc(t.status)}"></i></div><p>${esc(t.objective)}</p><p class="actions">${t.tools.map(tool=>esc(`${tool.connector_name} · ${tool.tool_name}`)).join('<br>')}</p>${t.dependency_failures.length?`<p class="error">${t.dependency_failures.map(esc).join(' · ')}</p>`:''}<div class="connector-actions">${t.status==='ready'?(t.active_job_count?`<button class="primary" type="button" disabled>Exécution en attente</button>`:`<button class="primary task-run" type="button" data-id="${esc(t.id)}">Exécuter maintenant</button>`):''}<button class="danger task-toggle" type="button" data-id="${esc(t.id)}" data-enabled="${t.enabled}">${t.enabled?'Mettre en pause':'Réactiver'}</button><button class="danger task-delete" type="button" data-id="${esc(t.id)}">Supprimer</button></div></article>`).join(''):empty('◇','Aucune tâche','Ajoutez au moins un outil pour créer votre première tâche.');document.querySelectorAll('.task-run').forEach(button=>button.onclick=async()=>{button.disabled=true;try{await api('/admin/api/v1/tasks/run',{method:'POST',body:JSON.stringify({task_id:button.dataset.id,input:{}})});await loadOperationalViews();showView('jobs')}catch(error){button.disabled=false;alert(`Exécution impossible : ${error.message}`)}});document.querySelectorAll('.task-toggle').forEach(button=>button.onclick=async()=>{try{await api('/admin/api/v1/tasks/enabled',{method:'POST',body:JSON.stringify({task_id:button.dataset.id,enabled:button.dataset.enabled!=='true'})});await loadTaskComposer()}catch(error){alert(`Modification impossible : ${error.message}`)}});document.querySelectorAll('.task-delete').forEach(button=>button.onclick=async()=>{if(confirm('Supprimer définitivement cette tâche configurée ?')){try{await api('/admin/api/v1/tasks/delete',{method:'POST',body:JSON.stringify({task_id:button.dataset.id})});await loadTaskComposer()}catch(error){alert(error.message==='task_in_use'?'Suppression impossible : cette tâche possède déjà des exécutions ou des rapports.':`Suppression impossible : ${error.message}`)}}})}
let schedulesCache=[];const scheduleForm=document.querySelector('#schedule-create'),scheduleKind=document.querySelector('#schedule-kind');
const scheduleText=s=>s.schedule_kind==='daily'?`chaque jour à ${s.time_of_day} (${s.timezone})`:s.schedule_kind==='weekly'?`chaque ${['lundi','mardi','mercredi','jeudi','vendredi','samedi','dimanche'][s.weekday]} à ${s.time_of_day} (${s.timezone})`:`toutes les ${s.interval_minutes} min`;
function updateScheduleFields(){const calendar=scheduleKind.value!=='interval';document.querySelector('#schedule-interval-label').hidden=calendar;document.querySelector('#schedule-calendar').hidden=!calendar;document.querySelector('#schedule-weekday-label').hidden=scheduleKind.value!=='weekly';scheduleForm.elements.time_of_day.required=calendar;scheduleForm.elements.timezone.required=calendar}
function resetScheduleForm(){scheduleForm.reset();scheduleForm.elements.schedule_id.value='';scheduleForm.elements.timezone.value=Intl.DateTimeFormat().resolvedOptions().timeZone||'UTC';document.querySelector('#schedule-form-title').textContent='Nouvelle planification';document.querySelector('#schedule-submit').textContent='Créer la planification';document.querySelector('#schedule-edit-cancel').hidden=true;updateScheduleFields()}
async function loadSchedules(){const [scheduleData,taskData]=await Promise.all([api('/admin/api/v1/schedules'),api('/admin/api/v1/tasks')]);schedulesCache=scheduleData.schedules;document.querySelector('#schedule-count').textContent=scheduleData.count;const taskSelect=document.querySelector('#schedule-task'),ready=taskData.tasks.filter(t=>t.status==='ready');taskSelect.innerHTML=ready.length?ready.map(t=>`<option value="${esc(t.id)}">${esc(t.display_name)}</option>`).join(''):'<option value="">Aucune tâche prête</option>';taskSelect.disabled=!ready.length;document.querySelector('#schedule-submit').disabled=!ready.length;document.querySelector('#schedule-list').innerHTML=schedulesCache.length?schedulesCache.map(s=>`<article class="identity"><div class="identityhead"><div><div class="identityname">${esc(s.display_name)}</div><div class="identitymeta">${esc(s.task_display_name)} · ${esc(scheduleText(s))}</div></div><i class="status-dot ${s.status==='active'?'ready':'disabled'}" role="img" aria-label="${esc(s.status)}" title="${esc(s.status)}"></i></div><p>Prochaine échéance : ${esc(date(s.next_run_at))}</p><p class="actions">État : ${esc(s.status)} · dernier passage : ${esc(date(s.last_run_at))} · ${esc(s.last_outcome||'aucun')}</p><div class="connector-actions"><button class="danger schedule-edit" data-id="${esc(s.id)}">Modifier</button><button class="danger schedule-toggle" data-id="${esc(s.id)}" data-enabled="${s.enabled}">${s.enabled?'Mettre en pause':'Réactiver'}</button><button class="danger schedule-delete" data-id="${esc(s.id)}">Supprimer</button></div></article>`).join(''):empty('◷','Aucune planification','Créez une récurrence pour alimenter automatiquement la file.');document.querySelectorAll('.schedule-edit').forEach(button=>button.onclick=()=>{const s=schedulesCache.find(item=>item.id===button.dataset.id);scheduleForm.elements.schedule_id.value=s.id;scheduleForm.elements.display_name.value=s.display_name;scheduleForm.elements.task_id.value=s.task_definition_id;scheduleForm.elements.schedule_kind.value=s.schedule_kind;scheduleForm.elements.interval_minutes.value=s.interval_minutes;scheduleForm.elements.time_of_day.value=s.time_of_day||'09:00';scheduleForm.elements.weekday.value=s.weekday??0;scheduleForm.elements.timezone.value=s.timezone||Intl.DateTimeFormat().resolvedOptions().timeZone;document.querySelector('#schedule-form-title').textContent='Modifier la planification';document.querySelector('#schedule-submit').textContent='Enregistrer';document.querySelector('#schedule-edit-cancel').hidden=false;updateScheduleFields();scheduleForm.scrollIntoView({behavior:'smooth'})});document.querySelectorAll('.schedule-toggle').forEach(button=>button.onclick=async()=>{await api('/admin/api/v1/schedules/enabled',{method:'POST',body:JSON.stringify({schedule_id:button.dataset.id,enabled:button.dataset.enabled!=='true'})});await loadSchedules()});document.querySelectorAll('.schedule-delete').forEach(button=>button.onclick=async()=>{if(confirm('Supprimer cette planification ?')){await api('/admin/api/v1/schedules/delete',{method:'POST',body:JSON.stringify({schedule_id:button.dataset.id})});await loadSchedules()}})}
let mappingsCache=[];async function loadMappings(){const [mappingData,taskData,identityData]=await Promise.all([api('/admin/api/v1/event-mappings'),api('/admin/api/v1/tasks'),api('/admin/api/v1/identities')]);mappingsCache=mappingData.event_mappings;document.querySelector('#mapping-count').textContent=mappingData.count;const sources=identityData.identities.filter(i=>i.identity_type==='event_source'&&i.status==='active'),tasks=taskData.tasks.filter(t=>t.status==='ready'),sourceSelect=document.querySelector('#mapping-source'),taskSelect=document.querySelector('#mapping-task');sourceSelect.innerHTML=sources.length?sources.map(i=>`<option value="${esc(i.id)}">${esc(i.display_name)}</option>`).join(''):'<option value="">Aucune source active</option>';taskSelect.innerHTML=tasks.length?tasks.map(t=>`<option value="${esc(t.id)}">${esc(t.display_name)}</option>`).join(''):'<option value="">Aucune tâche prête</option>';sourceSelect.disabled=!sources.length;taskSelect.disabled=!tasks.length;document.querySelector('#mapping-create button[type="submit"]').disabled=!sources.length||!tasks.length;document.querySelector('#mapping-list').innerHTML=mappingData.event_mappings.length?mappingData.event_mappings.map(m=>`<article class="identity"><div class="identityhead"><div><div class="identityname">${esc(m.display_name)}</div><div class="identitymeta">${esc(m.source_name)} · <code>${esc(m.event_type)}</code>${m.recovery_event_type?` / retour <code>${esc(m.recovery_event_type)}</code>`:''} → ${esc(m.task_display_name)}</div></div><i class="status-dot ${m.status==='active'?'ready':'disabled'}" role="img" aria-label="${esc(m.status)}" title="${esc(m.status)}"></i></div><p class="actions">État : ${esc(m.status)} · entrée : ${esc({full_event:'événement complet',subject:'sujet',attributes:'attributs'}[m.input_mode]||m.input_mode)} · grâce : ${m.grace_minutes?`${esc(m.grace_minutes)} min`:'aucune'} · cooldown : ${m.cooldown_minutes?`${esc(m.cooldown_minutes)} min`:'aucun'} · attente : ${esc(m.pending_due_at?`jusqu’au ${date(m.pending_due_at)}`:'aucune')} · dernier déclenchement : ${esc(date(m.last_triggered_at))}</p><div class="connector-actions"><button class="danger mapping-edit" data-id="${esc(m.id)}">Modifier</button><button class="danger mapping-toggle" data-id="${esc(m.id)}" data-enabled="${m.enabled}">${m.enabled?'Mettre en pause':'Réactiver'}</button><button class="danger mapping-delete" data-id="${esc(m.id)}">Supprimer</button></div></article>`).join(''):empty('↯','Aucun déclencheur','Associez une source et un type d’événement à une tâche.');
document.querySelectorAll('.mapping-edit').forEach(button=>button.onclick=()=>{const m=mappingsCache.find(item=>item.id===button.dataset.id),form=document.querySelector('#mapping-create');form.elements.mapping_id.value=m.id;form.elements.display_name.value=m.display_name;form.elements.source_identity_id.value=m.source_identity_id;form.elements.event_type.value=m.event_type;form.elements.task_id.value=m.task_definition_id;form.elements.input_mode.value=m.input_mode;form.elements.grace_minutes.value=m.grace_minutes;form.elements.recovery_event_type.value=m.recovery_event_type||'';form.elements.cooldown_minutes.value=m.cooldown_minutes;document.querySelector('#mapping-form-title').textContent='Modifier le déclencheur';document.querySelector('#mapping-submit').textContent='Enregistrer';document.querySelector('#mapping-edit-cancel').hidden=false;graceSelect.onchange();form.scrollIntoView({behavior:'smooth'})});document.querySelectorAll('.mapping-toggle').forEach(button=>button.onclick=async()=>{await api('/admin/api/v1/event-mappings/enabled',{method:'POST',body:JSON.stringify({mapping_id:button.dataset.id,enabled:button.dataset.enabled!=='true'})});await loadMappings()});document.querySelectorAll('.mapping-delete').forEach(button=>button.onclick=async()=>{if(confirm('Supprimer ce déclencheur ?')){await api('/admin/api/v1/event-mappings/delete',{method:'POST',body:JSON.stringify({mapping_id:button.dataset.id})});await loadMappings()}})}
function showView(name){const target=['overview','events','tasks','triggers','schedules','jobs','reports','connectors','audit'].includes(name)?name:'overview';localStorage.setItem('agw-view',target);document.querySelectorAll('.view').forEach(v=>v.classList.toggle('active',v.id===target));document.querySelectorAll('.nav [data-view]').forEach(a=>a.classList.toggle('active',a.dataset.view===target));if(location.hash!==`#${target}`)history.replaceState(null,'',`#${target}`)}
document.querySelectorAll('.nav [data-view]').forEach(a=>a.onclick=event=>{event.preventDefault();showView(a.dataset.view)});window.addEventListener('hashchange',()=>showView(location.hash.slice(1)));
async function refresh(){const [status,data]=await Promise.all([api('/admin/api/v1/status'),api('/admin/api/v1/identities')]);document.querySelector('#total').textContent=status.identities;document.querySelector('#active').textContent=status.active_identities;document.querySelector('#revoked').textContent=status.identities-status.active_identities;document.querySelector('#jobs-queued').textContent=status.queued_jobs;document.querySelector('#jobs-running').textContent=status.running_jobs;document.querySelector('#jobs-dead-letter').textContent=status.dead_letter_jobs;document.querySelector('#identity-count').textContent=status.identities;document.querySelector('#identities').innerHTML=data.identities.length?data.identities.map(i=>`<article class="identity"><div class="identityhead"><div><div class="identityname">${esc(i.display_name)}</div><div class="identitymeta">${esc(i.identity_type)} · créée ${esc(date(i.created_at))} · dernière utilisation ${esc(date(i.last_used_at))}</div></div><i class="status-dot ${esc(i.status)}" role="img" aria-label="${esc(i.status)}" title="${esc(i.status)}"></i></div><p class="actions">${esc(i.gateway_actions.join(' · ')||'Aucune permission')}</p>${i.status==='active'?`<button class="danger revoke" data-id="${esc(i.id)}">Révoquer</button>`:''}</article>`).join(''):'<div class="empty"><div class="emptyicon">◇</div><strong>Aucune identité</strong><p>Créez votre premier client MCP ou une source d’événements.</p></div>';document.querySelectorAll('.revoke').forEach(b=>b.onclick=async()=>{if(confirm('Révoquer immédiatement cette identité et tous ses identifiants ?')){await api('/admin/api/v1/identities/revoke',{method:'POST',body:JSON.stringify({identity_id:b.dataset.id})});refresh()}})}
document.querySelector('#create').onsubmit=async event=>{event.preventDefault();const message=document.querySelector('#message');message.textContent='';const form=new FormData(event.target),actions=form.getAll('actions');if(actions.includes('jobs.claim'))actions.push(...form.get('worker_actions').split(','));try{const result=await api('/admin/api/v1/identities',{method:'POST',body:JSON.stringify({display_name:form.get('display_name'),identity_type:form.get('identity_type'),actions})});const box=document.querySelector('#credential');box.classList.add('show');box.querySelector('code').textContent=result.credential;event.target.reset();await Promise.all([refresh(),loadMappings()])}catch(error){message.textContent=error.message}};
document.querySelector('#credential-dismiss').onclick=()=>{const box=document.querySelector('#credential');box.querySelector('code').textContent='';box.classList.remove('show')};
document.querySelectorAll('.job-filter').forEach(button=>button.onclick=async()=>{jobFilter=button.dataset.state;document.querySelectorAll('.job-filter').forEach(item=>item.classList.toggle('active',item===button));await loadJobs()});
document.querySelector('#connector-create').onsubmit=async event=>{event.preventDefault();const message=document.querySelector('#connector-message');message.textContent='Test de la connexion…';const form=new FormData(event.target);try{await api('/admin/api/v1/connectors',{method:'POST',body:JSON.stringify({display_name:form.get('display_name'),url:form.get('url'),bearer_token:form.get('bearer_token')})});message.textContent='';event.target.reset();await Promise.all([loadConnectors(),refresh()])}catch(error){message.textContent=error.message}};
document.querySelector('#task-create').onsubmit=async event=>{event.preventDefault();const message=document.querySelector('#task-message');message.textContent='';const form=new FormData(event.target),displayName=String(form.get('display_name')).trim(),name=displayName.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,'_').replace(/^_+|_+$/g,'').slice(0,120),tools=selectedTaskTools.map(({connector_id,tool_name})=>({connector_id,tool_name}));if(!name){message.textContent='Le nom doit contenir au moins une lettre ou un chiffre.';return}if(!tools.length){message.textContent='Ajoutez au moins un outil à la tâche.';return}try{await api('/admin/api/v1/tasks',{method:'POST',body:JSON.stringify({display_name:displayName,name,objective:form.get('objective'),max_attempts:Number(form.get('max_attempts')),tools})});event.target.reset();selectedTaskTools=[];await Promise.all([loadTaskComposer(),loadSchedules(),loadMappings()])}catch(error){message.textContent=error.message}};
scheduleKind.onchange=updateScheduleFields;document.querySelector('#schedule-edit-cancel').onclick=resetScheduleForm;resetScheduleForm();
document.querySelector('#schedule-create').onsubmit=async event=>{event.preventDefault();const message=document.querySelector('#schedule-message');message.textContent='';const form=new FormData(event.target),kind=form.get('schedule_kind'),payload={display_name:form.get('display_name'),task_id:form.get('task_id'),schedule_kind:kind,interval_minutes:kind==='interval'?Number(form.get('interval_minutes')):(kind==='daily'?1440:10080),time_of_day:kind==='interval'?null:form.get('time_of_day'),weekday:kind==='weekly'?Number(form.get('weekday')):null,timezone:kind==='interval'?null:form.get('timezone')},scheduleId=form.get('schedule_id');if(scheduleId)payload.schedule_id=scheduleId;try{await api(scheduleId?'/admin/api/v1/schedules/update':'/admin/api/v1/schedules',{method:'POST',body:JSON.stringify(payload)});resetScheduleForm();await loadSchedules()}catch(error){message.textContent=error.message}};
const graceSelect=document.querySelector('#mapping-grace'),recoveryLabel=document.querySelector('#mapping-recovery-label'),recoveryInput=recoveryLabel.querySelector('input');graceSelect.onchange=()=>{const enabled=Number(graceSelect.value)>0;recoveryLabel.hidden=!enabled;recoveryInput.required=enabled;if(!enabled)recoveryInput.value=''};
function resetMappingForm(){const form=document.querySelector('#mapping-create');form.reset();form.elements.mapping_id.value='';document.querySelector('#mapping-form-title').textContent='Nouveau déclencheur';document.querySelector('#mapping-submit').textContent='Créer le déclencheur';document.querySelector('#mapping-edit-cancel').hidden=true;graceSelect.onchange()}
document.querySelector('#mapping-edit-cancel').onclick=resetMappingForm;
document.querySelector('#mapping-create').onsubmit=async event=>{event.preventDefault();const message=document.querySelector('#mapping-message');message.textContent='';const form=new FormData(event.target),payload={display_name:form.get('display_name'),source_identity_id:form.get('source_identity_id'),event_type:form.get('event_type'),task_id:form.get('task_id'),cooldown_minutes:Number(form.get('cooldown_minutes')),grace_minutes:Number(form.get('grace_minutes')),recovery_event_type:form.get('recovery_event_type')||null,input_mode:form.get('input_mode')},mappingId=form.get('mapping_id');if(mappingId)payload.mapping_id=mappingId;try{await api(mappingId?'/admin/api/v1/event-mappings/update':'/admin/api/v1/event-mappings',{method:'POST',body:JSON.stringify(payload)});resetMappingForm();await loadMappings()}catch(error){message.textContent=error.message}};
showView(location.hash.slice(1)||localStorage.getItem('agw-view'));
Promise.all([refresh(),loadOperationalViews(),loadConnectors(),loadTaskComposer(),loadSchedules(),loadMappings()]).catch(error=>document.querySelector('#message').textContent=error.message);
"""
